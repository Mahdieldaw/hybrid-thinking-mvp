{
  "name": "hybrid-thinking-mvp",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/core-libs/common-types",
    "services/api-gateway",
    "apps/browser-extension",
    "apps/web-app",
    "cli"
  ],
  "scripts": {
    "build": "npm run build -w packages/core-libs/common-types && npm run build -w services/api-gateway && npm run build -w apps/browser-extension && npm run build -w apps/web-app && npm run build -w cli"
  }
}